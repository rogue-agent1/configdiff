# configdiff
Smart config file comparison for JSON, INI, and .env files.
```bash
python configdiff.py config.json config.prod.json
python configdiff.py .env .env.production
python configdiff.py old.ini new.ini --json
```
## Zero dependencies. Python 3.6+.
